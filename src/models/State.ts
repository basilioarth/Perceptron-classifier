export interface State {
    entry_one: number
    weight_one: number
    entry_two: number
    weight_two: number
    expected_class: boolean
    error?: number
    output?: number
    classification?: boolean
}